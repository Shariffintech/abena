function frameWorks (props){

    return (
        <div className="frameWorks">
            <p>{props.name}</p>
            <p>{props.description}</p>
        </div>
    )
}

export default frameWorks;